import { describe, it } from './util';
import { {{className}} } from '{{projectName}}';

describe('Math', () => {
it('should add two numbers correctly', () => {
const result = {{className}}.add(5, 7);
if (result !== 12) {
throw new Error(`Expected 12, got ${result}`);
}
});
});