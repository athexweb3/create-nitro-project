import { expect } from 'chai';
import { {{className}} } from '{{androidNamespace}}';

describe('{{className}}', () => {
it('should be structured correctly', () => {
expect({{className}}).to.be.an('object');
});

it('can add numbers', () => {
expect({{className}}.add(1, 2)).to.equal(3);
});
});