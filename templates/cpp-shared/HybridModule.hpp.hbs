#pragma once

#include "Hybrid{{className}}Spec.hpp"

namespace margelo::nitro::{{projectNamespace}} {

class Hybrid{{className}} : public Hybrid{{className}}Spec {
public:
Hybrid{{className}}() : HybridObject(TAG) {}

public:
double add(double a, double b) override;
};

}